<div class="nav">
    {{#nav}}
        <div class="{{menuClass}}" onclick="location.href='{{menuUrl}}';">{{menuClass}}</div>
    {{/nav}}
    {{^nav}}
        <p>저장된 Menu 데이터가 없습니다.</p>
    {{/nav}}
</div>